# System Instructions

## Task 01
    - create file catalog/product.py and write code to do following requirements:
        - auto generate product data
        - save into csv file base on path in config.json, such catalog_product_simple.csv
    - 
## Task 02
- create  file run.py if not exists in the root directory
- file run.py need to do following reqirements:
    - import the config.json file and read the configuration
    - load modules from the core directory
    - call the generate function of the module

